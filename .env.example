# Copy to .env.local for dev, or .env.production and upload to Vercel.
# .env, .env.local, .env.production are gitignored — never commit them.

MONGO_URL=mongodb://127.0.0.1:27017
DB_NAME=rapid_stage_scoring
ADMIN_USER=admin
ADMIN_PASS=admin123
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=8h
